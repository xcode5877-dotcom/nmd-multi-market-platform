# Order alarm sound

Place a loud, high-priority alarm file at **`alarm.mp3`** in this directory (or at `/alarm.mp3` from the app origin) for new-order notifications. If the file is missing, the app falls back to a short beep.
