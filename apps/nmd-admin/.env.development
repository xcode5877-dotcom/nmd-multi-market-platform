# UI on 5176, API on 5190. Use IP when accessing via network (avoids Failed to fetch).
VITE_MOCK_API_URL=http://147.93.120.244:5190
# JWT_SECRET is server-side only (mock-api). Token key: nmd-access-token
