node_modules
.env
.env.*
db.sqlite
coverage
.vscode

# Tooling / local caches (never commit)
.pnpm-store/
.gradle-local/
*.pid
.tmp-*.html

# Apple code signing — private keys must never be committed
certs/**/ios_distribution_private.key
*.p12
certs/**/*.base64.txt
*.mobileprovision

# Full backup lives on disk only (see BACKUP_MARCH_2026/README.md if present)
BACKUP_MARCH_2026/

# Generated artifacts, backups, and local temp (Tier A)
backups/
tmp/
**/build/
**/dist/
*.apk
*.aab
*.ipa
*.tar.gz
*.zip
*.jks.bak
*.pem
key.properties.bak
.env.local
.DS_Store
**/*.tsbuildinfo
*.log
apps/mock-api/uploads/*.apk
nmd-market-*.apk
soundmir.tar.gz
crm2.zip
